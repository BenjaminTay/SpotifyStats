# 账号中心页面实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新建 `/account` 账号中心页面，含 Hero 身份卡片 + 三个 Tab（你的收藏 / 你的习惯 / 你的身份），以人格叙事三层递进组织内容。

**Architecture:** 后端新增 `account_service.py` 提供收藏×播放交叉分析计算；新增 `account.py` API 路由聚合所有账号数据；前端新建 `AccountCenterPage` + `useAccount` hook + 类型定义，复用现有 GlassCard、KpiCard 等共享组件和 Tab 切换样式模式。

**Tech Stack:** FastAPI (Python) + React 19 + TypeScript + Tailwind CSS v4 + shadcn/ui

**Spec:** `docs/superpowers/specs/2026-05-30-account-center-design.md`

---

## 文件结构

```
Create:  backend/services/account_service.py    ← 收藏×播放交叉分析 + 聚合账号数据
Create:  backend/api/account.py                  ← 账号中心 API 路由
Modify:  backend/api/router.py                   ← 注册新路由
Create:  frontend/src/types/account.ts           ← 账号相关 TypeScript 类型
Create:  frontend/src/hooks/useAccount.ts         ← 账号中心数据 hook
Create:  frontend/src/pages/AccountCenterPage.tsx ← 主页面（Hero + Tab 切换）
Create:  frontend/src/pages/account/CollectionTab.tsx   ← Tab 1: 你的收藏
Create:  frontend/src/pages/account/HabitsTab.tsx       ← Tab 2: 你的习惯
Create:  frontend/src/pages/account/IdentityTab.tsx     ← Tab 3: 你的身份
Modify:  frontend/src/App.tsx                    ← 路由注册
Modify:  frontend/src/components/layout/Masthead.tsx ← 导航项
Modify:  frontend/src/lib/api.ts                 ← 类型重导出
```

---

### Task 1: 后端 — 账号中心服务层

**Files:**
- Create: `backend/services/account_service.py`
- 参考: `backend/services/library_service.py`, `backend/services/search_service.py`

- [ ] **Step 1: 创建服务文件，实现 `get_collection_insights()`**

```python
"""Account center services — cross-analysis of saved tracks × play history."""
import sqlite3
from datetime import date, timedelta
from collections import Counter

def get_collection_insights(conn: sqlite3.Connection) -> dict:
    """返回收藏×播放交叉分析的所有洞察数据。

    Returns dict with keys:
    - collection_personality: 收藏人格类型与指标
    - first_save_story: 第一首收藏的故事
    - lifecycle: 蜜月/冷却/沉淀/分化各阶段统计
    - chemistry: 6种关系类型分类及代表曲目
    - flip_side: 播放多但未收藏的曲目
    - keyword_migration: 按年收藏关键词变迁
    - co_saved_artists: 经常一起收藏的艺人组合
    - top_saved_artists: 收藏曲目最多艺人排行
    - mismatch: 收藏/播放错位榜
    - save_timeline: 年度收藏量
    - archive_facts: 收藏夹档案（总时长、年代跨度、语言）
    """
    cur = conn.execute("SELECT COUNT(*) FROM saved_tracks")
    saved_count = cur.fetchone()[0]
    if saved_count == 0:
        return {"available": False, "empty": True}

    # --- 1. 收藏人格 (Collection Personality) ---
    # 计算每首收藏曲目在收藏前的播放次数（中位数）
    cur = conn.execute("""
        WITH save_play_stats AS (
            SELECT
                st.track_name, st.artist_name, st.added_date,
                COUNT(CASE WHEN p.end_time < st.added_date THEN 1 END) as plays_before_save,
                COUNT(CASE WHEN p.end_time >= st.added_date THEN 1 END) as plays_after_save,
                COUNT(*) as total_plays
            FROM saved_tracks st
            LEFT JOIN tracks t ON st.track_uri = t.spotify_track_uri
            LEFT JOIN plays p ON p.track_id = t.id
            WHERE st.added_date IS NOT NULL AND st.added_date != ''
            GROUP BY st.track_name, st.artist_name
        )
        SELECT
            AVG(plays_before_save) as avg_before,
            AVG(plays_after_save) as avg_after,
            COUNT(CASE WHEN plays_after_save > 0 THEN 1 END) * 100.0 / COUNT(*) as retention_pct,
            COUNT(CASE WHEN plays_before_save <= 3 THEN 1 END) * 100.0 / COUNT(*) as impulsive_pct
        FROM save_play_stats
    """)
    row = cur.fetchone()
    avg_before = row[0] or 0
    retention_pct = row[2] or 0
    impulsive_pct = row[3] or 0

    # 人格判定
    if avg_before >= 10 and retention_pct >= 80:
        personality = {"type": "深海淘金者", "icon": "⛏️", "description": "你不会轻易收藏，平均听完多次后才按下那颗 ❤️。但一旦收藏，就几乎不再放手。"}
    elif avg_before <= 3 and retention_pct >= 70:
        personality = {"type": "冲动收藏家", "icon": "⚡", "description": "你相信第一感觉—绝大多数收藏都在 3 次播放内完成。"}
    elif avg_before >= 8 and saved_count < 500:
        personality = {"type": "精挑细选者", "icon": "💎", "description": "你的收藏夹小而精，每首歌都经过深思熟虑。"}
    else:
        personality = {"type": "均衡型收藏者", "icon": "🎵", "description": "你的收藏习惯介于冲动和谨慎之间，既有直觉选择也有深思熟虑。"}

    personality["metrics"] = {
        "avg_plays_before_save": round(avg_before, 1),
        "retention_pct": round(retention_pct, 1),
        "impulsive_pct": round(impulsive_pct, 1),
    }

    # --- 2. 收藏纵览 ---
    cur = conn.execute("SELECT COUNT(*) FROM saved_albums")
    saved_albums = cur.fetchone()[0]
    cur = conn.execute("SELECT COUNT(*) FROM saved_artists")
    saved_artists = cur.fetchone()[0]
    cur = conn.execute("SELECT COUNT(*) FROM playlists")
    playlist_count = cur.fetchone()[0]

    # 收藏时间线（按年）
    cur = conn.execute("""
        SELECT SUBSTR(added_date, 1, 4) as yr, COUNT(*) as cnt
        FROM saved_tracks WHERE added_date IS NOT NULL AND added_date != ''
        GROUP BY yr ORDER BY yr
    """)
    save_timeline = [{"year": int(r[0]), "count": r[1]} for r in cur.fetchall()]

    # 最大收藏日
    cur = conn.execute("""
        SELECT added_date, COUNT(*) as cnt FROM saved_tracks
        WHERE added_date IS NOT NULL AND added_date != ''
        GROUP BY added_date ORDER BY cnt DESC LIMIT 1
    """)
    max_save_day = cur.fetchone()
    biggest_save_day = {"date": max_save_day[0], "count": max_save_day[1]} if max_save_day else None

    # 首次和最近收藏
    cur = conn.execute("SELECT MIN(added_date), MAX(added_date) FROM saved_tracks WHERE added_date IS NOT NULL AND added_date != ''")
    first_last = cur.fetchone()

    overview = {
        "saved_tracks": saved_count,
        "saved_albums": saved_albums,
        "saved_artists": saved_artists,
        "playlists": playlist_count,
        "save_timeline": save_timeline,
        "biggest_save_day": biggest_save_day,
        "first_save_date": first_last[0],
        "last_save_date": first_last[1],
    }

    # --- 3. 第一首收藏的故事 ---
    cur = conn.execute("""
        SELECT st.track_name, st.artist_name, st.added_date,
               COUNT(p.id) as total_plays,
               CAST(julianday('now') - julianday(st.added_date) AS INTEGER) as days_since
        FROM saved_tracks st
        LEFT JOIN tracks t ON st.track_uri = t.spotify_track_uri
        LEFT JOIN plays p ON p.track_id = t.id
        WHERE st.added_date IS NOT NULL AND st.added_date != ''
        GROUP BY st.track_name, st.artist_name
        ORDER BY st.added_date ASC LIMIT 1
    """)
    row = cur.fetchone()
    first_save_story = None
    if row:
        days = row[4] or 0
        plays = row[3] or 0
        interval = round(days / max(plays, 1), 1)
        first_save_story = {
            "track_name": row[0], "artist_name": row[1],
            "save_date": row[2], "total_plays": plays,
            "days_since": days, "avg_interval_days": interval,
        }

    # --- 4. 收藏生命周期 ---
    cur = conn.execute("""
        WITH save_week_stats AS (
            SELECT
                st.track_name, st.artist_name, st.added_date,
                COUNT(CASE WHEN p.end_time >= st.added_date AND p.end_time < date(st.added_date, '+7 days') THEN 1 END) as wk1,
                COUNT(CASE WHEN p.end_time >= date(st.added_date, '+30 days') AND p.end_time < date(st.added_date, '+90 days') THEN 1 END) as mo1_3,
                COUNT(CASE WHEN p.end_time >= date(st.added_date, '+90 days') AND p.end_time < date(st.added_date, '+365 days') THEN 1 END) as mo3_12,
                COUNT(CASE WHEN p.end_time >= date(st.added_date, '+365 days') THEN 1 END) as after_1yr
            FROM saved_tracks st
            LEFT JOIN tracks t ON st.track_uri = t.spotify_track_uri
            LEFT JOIN plays p ON p.track_id = t.id
            WHERE st.added_date IS NOT NULL AND st.added_date != ''
            GROUP BY st.track_name, st.artist_name
        )
        SELECT
            ROUND(AVG(wk1), 1) as avg_honeymoon,
            ROUND(AVG(mo1_3 * 1.0 / 8), 1) as avg_cooling,  -- per week over ~8 weeks
            ROUND(AVG(mo3_12 * 1.0 / 39), 1) as avg_settling, -- per week over ~39 weeks
            COUNT(CASE WHEN after_1yr > 0 AND mo3_12 > 0 THEN 1 END) * 100.0 / COUNT(*) as evergreen_pct,
            COUNT(CASE WHEN after_1yr = 0 AND mo3_12 > 0 THEN 1 END) * 100.0 / COUNT(*) as occasional_pct,
            COUNT(CASE WHEN after_1yr = 0 AND mo3_12 = 0 AND mo1_3 = 0 THEN 1 END) * 100.0 / COUNT(*) as forgotten_pct
        FROM save_week_stats
    """)
    row = cur.fetchone()
    lifecycle = {
        "honeymoon": {"label": "蜜月期", "weeks": "0-1", "avg_per_week": row[0] or 0},
        "cooling": {"label": "冷却期", "weeks": "1-12", "avg_per_week": row[1] or 0},
        "settling": {"label": "沉淀期", "weeks": "12-52", "avg_per_week": row[2] or 0},
        "fate": {"evergreen_pct": round(row[3] or 0, 1), "occasional_pct": round(row[4] or 0, 1), "forgotten_pct": round(row[5] or 0, 1)},
    }

    # --- 5. 收藏×播放 化学反应（6 种类型） ---
    cur = conn.execute("""
        WITH stats AS (
            SELECT
                st.track_name, st.artist_name, st.added_date,
                COUNT(CASE WHEN p.end_time < st.added_date THEN 1 END) as before_save,
                COUNT(CASE WHEN p.end_time >= st.added_date AND p.end_time < date(st.added_date, '+7 days') THEN 1 END) as first_week,
                COUNT(CASE WHEN p.end_time >= st.added_date AND p.end_time < date(st.added_date, '+180 days') THEN 1 END) as first_6mo,
                COUNT(CASE WHEN p.end_time >= date(st.added_date, '+180 days') THEN 1 END) as after_6mo,
                COUNT(CASE WHEN p.end_time >= date(st.added_date, '+90 days') THEN 1 END) as recent_plays,
                MAX(p.end_time) as last_play,
                COUNT(*) as total_plays
            FROM saved_tracks st
            LEFT JOIN tracks t ON st.track_uri = t.spotify_track_uri
            LEFT JOIN plays p ON p.track_id = t.id
            WHERE st.added_date IS NOT NULL AND st.added_date != ''
            GROUP BY st.track_name, st.artist_name
        )
        SELECT
            -- 一见钟情: 收藏时播放 <= 3
            COUNT(CASE WHEN before_save <= 3 THEN 1 END) as love_at_first,
            -- 慢热型: 收藏时播放 >= 20
            COUNT(CASE WHEN before_save >= 20 THEN 1 END) as slow_burn,
            -- 昙花一现: 收藏周 >= 10 且超过 3 月未播
            COUNT(CASE WHEN first_week >= 10 AND (last_play < date('now', '-90 days') OR last_play IS NULL) THEN 1 END) as flash_pan,
            -- 厚积薄发: 收藏后 6 个月播放 > 前 6 个月播放 * 2
            COUNT(CASE WHEN after_6mo > first_6mo * 2 AND first_6mo > 0 THEN 1 END) as late_bloomer,
            -- 细水长流: 收藏前后播放波动 < 30%（用前后均值比）
            COUNT(CASE WHEN before_save > 0 AND first_6mo > 0
                  AND ABS(1.0 * first_6mo / NULLIF(before_save, 0) - 1) < 0.5 THEN 1 END) as steady,
            -- 吃灰: 总播放 <= 3
            COUNT(CASE WHEN total_plays <= 3 THEN 1 END) as shelf_sitter,
            COUNT(*) as total
        FROM stats
    """)
    row = cur.fetchone()
    total_with_dates = row[6]

    # 获取每种类型的代表曲目
    def _fetch_examples(condition_sql, limit=3):
        cur = conn.execute(f"""
            WITH stats AS (
                SELECT st.track_name, st.artist_name, st.added_date,
                    COUNT(CASE WHEN p.end_time < st.added_date THEN 1 END) as before_save,
                    COUNT(CASE WHEN p.end_time >= st.added_date AND p.end_time < date(st.added_date, '+7 days') THEN 1 END) as first_week,
                    COUNT(CASE WHEN p.end_time >= st.added_date AND p.end_time < date(st.added_date, '+180 days') THEN 1 END) as first_6mo,
                    COUNT(CASE WHEN p.end_time >= date(st.added_date, '+180 days') THEN 1 END) as after_6mo,
                    MAX(p.end_time) as last_play,
                    COUNT(*) as total_plays,
                    CAST(julianday('now') - julianday(MAX(p.end_time)) AS INTEGER) as days_since_play
                FROM saved_tracks st
                LEFT JOIN tracks t ON st.track_uri = t.spotify_track_uri
                LEFT JOIN plays p ON p.track_id = t.id
                WHERE st.added_date IS NOT NULL AND st.added_date != ''
                GROUP BY st.track_name, st.artist_name
            )
            SELECT track_name, artist_name, total_plays, before_save, first_week, days_since_play
            FROM stats
            WHERE {condition_sql}
            ORDER BY total_plays DESC
            LIMIT {limit}
        """)
        return [{"track_name": r[0], "artist_name": r[1], "total_plays": r[2],
                 "before_save": r[3], "first_week": r[4], "days_since_play": r[5]} for r in cur.fetchall()]

    chemistry = {
        "love_at_first_listen": {
            "count": row[0] or 0, "label": "一见钟情",
            "description": "收藏时播放次数 ≤ 3", "icon": "💘",
            "examples": _fetch_examples("before_save <= 3"),
        },
        "slow_burn": {
            "count": row[1] or 0, "label": "慢热型",
            "description": "收藏时已播放 ≥ 20 次", "icon": "🔥",
            "examples": _fetch_examples("before_save >= 20"),
        },
        "flash_in_the_pan": {
            "count": row[2] or 0, "label": "昙花一现",
            "description": "收藏周播放 ≥ 10 次，现已 > 3 月未播", "icon": "🌠",
            "examples": _fetch_examples("first_week >= 10 AND (last_play < date('now', '-90 days') OR last_play IS NULL)"),
        },
        "late_bloomer": {
            "count": row[3] or 0, "label": "厚积薄发",
            "description": "收藏后 6 个月播放持续增长", "icon": "🌱",
            "examples": _fetch_examples("after_6mo > first_6mo * 2 AND first_6mo > 0"),
        },
        "steady_favorite": {
            "count": row[4] or 0, "label": "细水长流",
            "description": "收藏前后播放频率稳定", "icon": "💪",
            "examples": _fetch_examples("before_save > 0 AND first_6mo > 0 AND ABS(1.0 * first_6mo / NULLIF(before_save, 0) - 1) < 0.5"),
        },
        "shelf_sitter": {
            "count": row[5] or 0, "label": "收藏夹吃灰",
            "description": "收藏后总播放 ≤ 3 次", "icon": "📌",
            "examples": _fetch_examples("total_plays <= 3"),
        },
        "total_with_dates": total_with_dates,
    }

    # --- 6. Flip Side: 播放多但未收藏 ---
    cur = conn.execute("""
        SELECT p_agg.track_name, p_agg.artist_name, p_agg.play_count
        FROM (
            SELECT t.track_name, a.artist_name, COUNT(*) as play_count
            FROM plays p
            JOIN tracks t ON p.track_id = t.id
            JOIN artists a ON t.artist_id = a.id
            WHERE p.track_id IS NOT NULL
            GROUP BY t.track_name, a.artist_name
            HAVING play_count >= 50
        ) p_agg
        LEFT JOIN saved_tracks st
            ON st.track_name = p_agg.track_name AND st.artist_name = p_agg.artist_name
        WHERE st.track_uri IS NULL
        ORDER BY p_agg.play_count DESC
        LIMIT 10
    """)
    flip_side = [{"track_name": r[0], "artist_name": r[1], "play_count": r[2]} for r in cur.fetchall()]

    # --- 7. 收藏关键词变迁 ---
    cur = conn.execute("""
        SELECT SUBSTR(added_date, 1, 4) as yr, GROUP_CONCAT(track_name, ' ') as all_names
        FROM saved_tracks
        WHERE added_date IS NOT NULL AND added_date != ''
        GROUP BY yr ORDER BY yr
    """)
    # 简单高频词提取（英文单词 + 中文字符）
    keyword_migration = {}
    common_words = {"the", "a", "an", "is", "in", "of", "to", "and", "for", "on", "it", "remix", "feat", "mix", "edit", "version", "original"}
    for yr, names in cur.fetchall():
        words = names.lower().split()
        filtered = [w for w in words if len(w) > 2 and w not in common_words]
        top = [w for w, _ in Counter(filtered).most_common(5)]
        keyword_migration[yr] = top

    # --- 8. 双厨时刻 ---
    cur = conn.execute("""
        SELECT st1.artist_name as a1, st2.artist_name as a2, st1.added_date, COUNT(*) as cnt
        FROM saved_tracks st1
        JOIN saved_tracks st2 ON st1.added_date = st2.added_date AND st1.artist_name < st2.artist_name
        WHERE st1.added_date IS NOT NULL AND st1.added_date != ''
        GROUP BY a1, a2
        ORDER BY cnt DESC
        LIMIT 10
    """)
    co_saved = [{"artist_a": r[0], "artist_b": r[1], "count": r[3]} for r in cur.fetchall()]

    # --- 9. 排行榜 ---
    cur = conn.execute("""
        SELECT st.artist_name, COUNT(*) as saved_cnt,
               COALESCE(SUM(p_stats.play_count), 0) as total_plays
        FROM saved_tracks st
        LEFT JOIN (
            SELECT t.track_name, a.artist_name, COUNT(*) as play_count
            FROM plays p JOIN tracks t ON p.track_id = t.id
            JOIN artists a ON t.artist_id = a.id
            GROUP BY t.track_name, a.artist_name
        ) p_stats ON st.track_name = p_stats.track_name AND st.artist_name = p_stats.artist_name
        GROUP BY st.artist_name
        ORDER BY saved_cnt DESC
        LIMIT 15
    """)
    top_saved_artists = [{"artist_name": r[0], "saved_count": r[1], "total_plays": r[2]} for r in cur.fetchall()]

    # 错位榜
    mismatch = {"over_saved": [], "under_saved": []}
    for a in top_saved_artists:
        if a["saved_count"] >= 5 and a["total_plays"] < a["saved_count"] * 3:
            mismatch["over_saved"].append(a)
        if a["total_plays"] >= 50 and a["saved_count"] <= 2:
            mismatch["under_saved"].append(a)
    mismatch["over_saved"] = mismatch["over_saved"][:5]
    mismatch["under_saved"] = mismatch["under_saved"][:5]

    # --- 10. 收藏夹档案 ---
    # 总时长估算（假设平均每首 3.5 分钟）
    total_duration_min = saved_count * 3.5
    total_duration_hrs = round(total_duration_min / 60, 1)

    # 年代跨度（从 tracks 表中查找 saved_tracks 里最早和最晚的发行年份）
    cur = conn.execute("""
        SELECT MIN(t.release_year), MAX(t.release_year)
        FROM saved_tracks st
        JOIN tracks t ON st.track_uri = t.spotify_track_uri
    """)
    year_range = cur.fetchone()

    archive_facts = {
        "total_duration_hrs": total_duration_hrs,
        "year_span": f"{year_range[0]} – {year_range[1]}" if year_range[0] else None,
        "oldest_track": None,
    }
    if year_range[0]:
        cur = conn.execute("""
            SELECT st.track_name, st.artist_name, t.release_year
            FROM saved_tracks st JOIN tracks t ON st.track_uri = t.spotify_track_uri
            ORDER BY t.release_year ASC LIMIT 1
        """)
        oldest = cur.fetchone()
        if oldest:
            archive_facts["oldest_track"] = {"track_name": oldest[0], "artist_name": oldest[1], "year": oldest[2]}

    return {
        "available": True,
        "empty": False,
        "personality": personality,
        "overview": overview,
        "first_save_story": first_save_story,
        "lifecycle": lifecycle,
        "chemistry": chemistry,
        "flip_side": flip_side,
        "keyword_migration": keyword_migration,
        "co_saved_artists": co_saved,
        "top_saved_artists": top_saved_artists,
        "mismatch": mismatch,
        "archive_facts": archive_facts,
    }
```

- [ ] **Step 2: 实现 `get_account_summary()` 聚合方法**

```python
def get_account_summary(conn: sqlite3.Connection) -> dict:
    """聚合账号数据入口 — 合并 library + search + insights 的关键数据。

    返回与 useAccount hook 需要的数据结构对齐的汇总对象。
    """
    from backend.services.library_service import get_library_overview
    from backend.services.profile_service import get_profile
    from backend.services.search_service import get_search_stats
    from backend.services.insights_service import get_artist_tiers, get_marquee_stats
    from backend.services.podcast_service import get_podcast_overview
    from backend.services.video_service import get_video_stats

    try:
        library = get_library_overview(conn)
    except Exception:
        library = {"available": False}
    try:
        profile = get_profile(conn)
    except Exception:
        profile = {"profile": {}, "follows": [], "prompts": [], "stats": {}, "banned_items": []}
    try:
        search = get_search_stats(conn)
    except Exception:
        search = {"available": False}
    try:
        insights_tiers = get_artist_tiers(conn)
    except Exception:
        insights_tiers = {"available": False}
    try:
        insights_marquee = get_marquee_stats(conn)
    except Exception:
        insights_marquee = {"available": False}
    try:
        podcast = get_podcast_overview(conn)
    except Exception:
        podcast = {"available": False}
    try:
        video = get_video_stats(conn)
    except Exception:
        video = {"available": False}
    try:
        collection = get_collection_insights(conn)
    except Exception:
        collection = {"available": False}

    has_account_data = library.get("available", False)

    return {
        "has_account_data": has_account_data,
        "profile": profile,
        "library": library,
        "collection_insights": collection,
        "search": search,
        "insights_tiers": insights_tiers,
        "insights_marquee": insights_marquee,
        "podcast": podcast,
        "video": video,
    }
```

- [ ] **Step 3: 验证服务可导入**

```bash
source .venv/bin/activate && python3 -c "from backend.services.account_service import get_collection_insights, get_account_summary; print('OK')"
```

- [ ] **Step 4: 提交**

```bash
git add backend/services/account_service.py
git commit -m "$(cat <<'EOF'
feat: 新增账号中心服务层

- 新增 account_service.py，实现收藏×播放交叉分析
- get_collection_insights(): 收藏人格、生命周期、化学反应、双厨时刻等 10 个洞察维度
- get_account_summary(): 聚合 library/search/insights/podcast/video/profile 所有账号数据
EOF
)"
```

---

### Task 2: 后端 — 账号中心 API 路由

**Files:**
- Create: `backend/api/account.py`
- Modify: `backend/api/router.py`

- [ ] **Step 1: 创建 API 路由文件**

```python
"""Account center API — aggregated endpoints for the /account page."""
from fastapi import APIRouter, Depends
from sqlite3 import Connection

from backend.dependencies import get_conn
from backend.services.account_service import get_collection_insights, get_account_summary

router = APIRouter(prefix="/account", tags=["Account"])


@router.get("")
def account_summary(conn: Connection = Depends(get_conn)):
    """聚合账号中心所有数据（library + search + insights + podcast + video + profile + collection insights）。"""
    return get_account_summary(conn)


@router.get("/collection-insights")
def collection_insights(conn: Connection = Depends(get_conn)):
    """收藏×播放交叉分析洞察。"""
    return get_collection_insights(conn)
```

- [ ] **Step 2: 注册路由到 api_router**

在 `backend/api/router.py` 中添加：

```python
from backend.api.account import router as account_router

# 在 api_router 定义后面添加：
api_router.include_router(account_router)
```

- [ ] **Step 3: 验证 API 可访问**

```bash
source .venv/bin/activate && uvicorn backend.main:app &
sleep 3
curl -s http://localhost:8000/api/account | python3 -m json.tool | head -20
```

- [ ] **Step 4: 提交**

```bash
git add backend/api/account.py backend/api/router.py
git commit -m "$(cat <<'EOF'
feat: 新增账号中心 API 路由

- GET /api/account — 聚合所有账号数据的统一入口
- GET /api/account/collection-insights — 收藏×播放交叉分析
EOF
)"
```

---

### Task 3: 前端 — TypeScript 类型定义

**Files:**
- Create: `frontend/src/types/account.ts`

- [ ] **Step 1: 创建类型文件**

```typescript
/* Account Center types */

// --- Collection Tab ---

export interface CollectionPersonality {
  type: string
  icon: string
  description: string
  metrics: {
    avg_plays_before_save: number
    retention_pct: number
    impulsive_pct: number
  }
}

export interface SaveTimelinePoint {
  year: number
  count: number
}

export interface BiggestSaveDay {
  date: string
  count: number
}

export interface CollectionOverview {
  saved_tracks: number
  saved_albums: number
  saved_artists: number
  playlists: number
  save_timeline: SaveTimelinePoint[]
  biggest_save_day: BiggestSaveDay | null
  first_save_date: string | null
  last_save_date: string | null
}

export interface FirstSaveStory {
  track_name: string
  artist_name: string
  save_date: string
  total_plays: number
  days_since: number
  avg_interval_days: number
}

export interface LifecycleStage {
  label: string
  weeks: string
  avg_per_week: number
}

export interface LifecycleFate {
  evergreen_pct: number
  occasional_pct: number
  forgotten_pct: number
}

export interface SaveLifecycle {
  honeymoon: LifecycleStage
  cooling: LifecycleStage
  settling: LifecycleStage
  fate: LifecycleFate
}

export interface ChemistryExample {
  track_name: string
  artist_name: string
  total_plays: number
  before_save: number
  first_week: number
  days_since_play: number | null
}

export interface ChemistryType {
  count: number
  label: string
  description: string
  icon: string
  examples: ChemistryExample[]
}

export interface SavePlayChemistry {
  love_at_first_listen: ChemistryType
  slow_burn: ChemistryType
  flash_in_the_pan: ChemistryType
  late_bloomer: ChemistryType
  steady_favorite: ChemistryType
  shelf_sitter: ChemistryType
  total_with_dates: number
}

export interface FlipSideTrack {
  track_name: string
  artist_name: string
  play_count: number
}

export interface CoSavedPair {
  artist_a: string
  artist_b: string
  count: number
}

export interface TopSavedArtist {
  artist_name: string
  saved_count: number
  total_plays: number
}

export interface MismatchArtists {
  over_saved: TopSavedArtist[]
  under_saved: TopSavedArtist[]
}

export interface ArchiveFacts {
  total_duration_hrs: number
  year_span: string | null
  oldest_track: { track_name: string; artist_name: string; year: number } | null
}

export interface CollectionInsights {
  available: boolean
  empty?: boolean
  personality: CollectionPersonality
  overview: CollectionOverview
  first_save_story: FirstSaveStory | null
  lifecycle: SaveLifecycle
  chemistry: SavePlayChemistry
  flip_side: FlipSideTrack[]
  keyword_migration: Record<string, string[]>
  co_saved_artists: CoSavedPair[]
  top_saved_artists: TopSavedArtist[]
  mismatch: MismatchArtists
  archive_facts: ArchiveFacts
}

// --- Habits Tab ---

export interface SearchData {
  available: boolean
  empty?: boolean
  total_searches: number
  daily_volume: { date: string; count: number }[]
  top_queries: { query: string; count: number }[]
  intent_dist: { intent: string; count: number }[]
  heatmap: {
    z: number[][]
    x: number[]
    y: string[]
  }
}

export interface ArtistTier {
  rank: number
  artist_name: string
  play_count: number
  hours: number
  tier: string
}

export interface ArtistTiersData {
  available: boolean
  empty?: boolean
  total_artists: number
  tier_hours: Record<string, number>
  tier_counts: Record<string, number>
  artists: ArtistTier[]
}

export interface MarqueeConversion {
  artist_name: string
  segment: string
  impressions: number
  actual_plays: number
  actual_hours: number
}

export interface MarqueeData {
  available: boolean
  empty?: boolean
  conversions: MarqueeConversion[]
}

export interface PodcastShow {
  show_name: string
  hours: number
}

export interface PodcastMonthlyPoint {
  period: string
  hours: number
}

export interface PodcastData {
  available: boolean
  empty?: boolean
  total_plays: number
  total_hours: number
  unique_shows: number
  saved_shows: number
  top_shows: PodcastShow[]
  monthly_trend: PodcastMonthlyPoint[]
}

export interface VideoTrack {
  track_name: string
  artist_name: string
  video_plays: number
  audio_plays: number
}

export interface VideoYearlyPoint {
  year: number
  audio: number
  video: number
}

export interface VideoData {
  available: boolean
  empty?: boolean
  total_video_plays: number
  total_audio_plays: number
  avg_duration_sec: number
  platform_dist: Record<string, number>
  yearly: VideoYearlyPoint[]
  top_video_tracks: VideoTrack[]
}

// --- Identity Tab ---

export interface UserProfile {
  identity_displayName?: string
  identity_firstName?: string
  identity_lastName?: string
  identity_imageUrl?: string
  attr_username?: string
  attr_country?: string
  attr_birthdate?: string
  [key: string]: string | undefined
}

export interface UserFollow {
  type: string
  name: string
}

export interface UserPrompt {
  message: string
  created: string
}

export interface BannedItem {
  name: string
  type: string
}

export interface ProfileStats {
  first_play_date: string | null
  total_audio_plays: number
}

export interface ProfileData {
  profile: UserProfile
  follows: UserFollow[]
  prompts: UserPrompt[]
  stats: ProfileStats
  banned_items: BannedItem[]
}

// --- Wrapped Hub (reused in Identity Tab) ---
// These types mirror wrapped-hub response; we define a subset here
export interface WrappedClub {
  club_name: string
  percent_in_club: number
  role: string
  artist_name: string
}

export interface WrappedPartyMetric {
  metric: string
  value: number
}

export interface WrappedListeningAge {
  age: number
  window_start_year: number
  decade_phase: string
}

export interface WrappedArchiveReport {
  column_qualifier: string
  title: string
  description: string
  reason: string
  minutes_listened: number
  filed_under_tags: string
}

export interface WrappedHubData {
  available: boolean
  empty?: boolean
  top_artists: { rank: number; name: string; ms_played: number; percentile: number; cover_url: string }[]
  top_tracks: { rank: number; name: string; play_count: number; ms_played: number; cover_url: string }[]
  top_albums: { rank: number; name: string; play_count: number; ms_played: number; cover_url: string }[]
  clubs: WrappedClub[]
  party_metrics: WrappedPartyMetric[]
  listening_age: WrappedListeningAge | null
  archive_reports: WrappedArchiveReport[]
}

// --- Aggregated Account Data ---

export interface AccountSummary {
  has_account_data: boolean
  profile: ProfileData
  library: {
    available: boolean
    saved_tracks?: number
    saved_albums?: number
    saved_artists?: number
    playlists?: number
    banned_items?: number
    artist_comparison?: { artist_name: string; saved_count: number; play_count: number }[]
    forgotten_tracks?: { track_id: string; never_played: boolean; track_name: string; artist_name: string }[]
  }
  collection_insights: CollectionInsights
  search: SearchData
  insights_tiers: ArtistTiersData
  insights_marquee: MarqueeData
  podcast: PodcastData
  video: VideoData
}
```

- [ ] **Step 2: 验证类型文件无语法错误**

```bash
cd frontend && npx tsc --noEmit src/types/account.ts
```

- [ ] **Step 3: 在 api.ts 中重导出类型**

在 `frontend/src/lib/api.ts` 中添加：

```typescript
export type {
  AccountSummary,
  CollectionInsights,
  CollectionPersonality,
  CollectionOverview,
  FirstSaveStory,
  SaveLifecycle,
  SavePlayChemistry,
  ChemistryType,
  FlipSideTrack,
  CoSavedPair,
  TopSavedArtist,
  SearchData,
  ArtistTiersData,
  MarqueeData,
  PodcastData,
  VideoData,
  ProfileData,
  WrappedHubData,
} from '@/types/account'
```

- [ ] **Step 4: 提交**

```bash
git add frontend/src/types/account.ts frontend/src/lib/api.ts
git commit -m "$(cat <<'EOF'
feat: 新增账号中心 TypeScript 类型定义

- account.ts: CollectionInsights、SearchData、ProfileData 等完整类型
- api.ts: 重导出 AccountSummary 等类型
EOF
)"
```

---

### Task 4: 前端 — useAccount Hook

**Files:**
- Create: `frontend/src/hooks/useAccount.ts`
- 参考: `frontend/src/hooks/useSettings.ts`, `frontend/src/hooks/useYearlyReview.ts`

- [ ] **Step 1: 创建 hook 文件**

```typescript
import { useState, useEffect, useCallback } from 'react'
import { api, type AccountSummary } from '@/lib/api'

let cachedData: AccountSummary | null = null
let inFlight: Promise<AccountSummary> | null = null

interface UseAccountResult {
  data: AccountSummary | null
  loading: boolean
  error: string | null
  refetch: () => void
}

export function useAccount(): UseAccountResult {
  const [data, setData] = useState<AccountSummary | null>(cachedData)
  const [loading, setLoading] = useState(!cachedData)
  const [error, setError] = useState<string | null>(null)

  const refetch = useCallback(() => {
    setLoading(true)
    setError(null)

    if (inFlight) {
      inFlight
        .then((result) => {
          cachedData = result
          setData(result)
          setLoading(false)
        })
        .catch((e: Error) => {
          setError(e.message)
          setLoading(false)
        })
      return
    }

    const promise = api.get<AccountSummary>('/account')
    inFlight = promise

    promise
      .then((result) => {
        cachedData = result
        if (inFlight === promise) {
          setData(result)
          setError(null)
        }
      })
      .catch((e: Error) => {
        if (inFlight === promise) {
          setError(e.message)
        }
      })
      .finally(() => {
        if (inFlight === promise) {
          inFlight = null
          setLoading(false)
        }
      })
  }, [])

  useEffect(() => {
    if (!cachedData) {
      refetch()
    }
  }, [refetch])

  return { data, loading, error, refetch }
}

/**
 * 预取账号中心数据（用于 Masthead hover 或布局预加载）。
 * 不触发组件更新，只填充模块级缓存。
 */
export function prefetchAccount(): void {
  if (cachedData || inFlight) return
  const promise = api.get<AccountSummary>('/account')
  inFlight = promise
  promise
    .then((result) => {
      cachedData = result
    })
    .catch(() => {
      // prefetch failure is silent
    })
    .finally(() => {
      inFlight = null
    })
}
```

- [ ] **Step 2: 验证 hook 无类型错误**

```bash
cd frontend && npx tsc --noEmit src/hooks/useAccount.ts
```

- [ ] **Step 3: 提交**

```bash
git add frontend/src/hooks/useAccount.ts
git commit -m "$(cat <<'EOF'
feat: 新增 useAccount hook

- 模块级缓存 + in-flight 去重
- refetch / prefetch 支持
- 遵循 useSettings / useYearlyReview 既有模式
EOF
)"
```

---

### Task 5: 前端 — 路由和导航注册

**Files:**
- Modify: `frontend/src/App.tsx`
- Modify: `frontend/src/components/layout/Masthead.tsx`

- [ ] **Step 1: 在 App.tsx 添加懒加载和路由**

懒加载 import（放在其他 lazy import 之后）：
```typescript
const AccountCenterPage = lazy(() => import('@/pages/AccountCenterPage').then((m) => ({ default: m.AccountCenterPage })))
```

路由（放在 settings 路由之后）：
```tsx
<Route path="/account" element={<Suspense fallback={<RouteFallback />}><AccountCenterPage /></Suspense>} />
```

- [ ] **Step 2: 在 Masthead.tsx 添加导航项**

在 `NAV_ITEMS` 数组中添加（放在 settings 之前）：
```typescript
{ to: '/account', label: '账户' },
```

- [ ] **Step 3: 验证前端编译通过**

```bash
cd frontend && npx tsc --noEmit
```

- [ ] **Step 4: 提交**

```bash
git add frontend/src/App.tsx frontend/src/components/layout/Masthead.tsx
git commit -m "$(cat <<'EOF'
feat: 注册账号中心路由和导航

- App.tsx: 懒加载 AccountCenterPage，挂载到 /account
- Masthead.tsx: 导航栏新增「账户」入口
EOF
)"
```

---

### Task 6: 前端 — AccountCenterPage 主页面骨架

**Files:**
- Create: `frontend/src/pages/AccountCenterPage.tsx`
- Create: `frontend/src/pages/account/CollectionTab.tsx` (占位)
- Create: `frontend/src/pages/account/HabitsTab.tsx` (占位)
- Create: `frontend/src/pages/account/IdentityTab.tsx` (占位)
- 参考: `frontend/src/pages/YearlyReviewPage.tsx` (Tab 切换模式)

- [ ] **Step 1: 创建三个 Tab 占位组件**

`frontend/src/pages/account/CollectionTab.tsx`:
```typescript
import type { CollectionInsights } from '@/types/account'

export function CollectionTab({ insights }: { insights: CollectionInsights }) {
  return (
    <div className="space-y-6">
      <p className="font-serif text-2xl font-semibold">📁 你的收藏</p>
      <p className="text-muted-foreground">收藏×播放交叉分析即将上线...</p>
    </div>
  )
}
```

`frontend/src/pages/account/HabitsTab.tsx`:
```typescript
import type { SearchData, ArtistTiersData, MarqueeData, PodcastData, VideoData } from '@/types/account'

interface HabitsTabProps {
  search: SearchData
  tiers: ArtistTiersData
  marquee: MarqueeData
  podcast: PodcastData
  video: VideoData
}

export function HabitsTab({ search, tiers, marquee, podcast, video }: HabitsTabProps) {
  return (
    <div className="space-y-6">
      <p className="font-serif text-2xl font-semibold">🔍 你的习惯</p>
      <p className="text-muted-foreground">搜索/粉丝/播客/视频分析即将上线...</p>
    </div>
  )
}
```

`frontend/src/pages/account/IdentityTab.tsx`:
```typescript
import type { ProfileData, WrappedHubData } from '@/types/account'

interface IdentityTabProps {
  profile: ProfileData
  wrappedHub: WrappedHubData | null
}

export function IdentityTab({ profile, wrappedHub }: IdentityTabProps) {
  return (
    <div className="space-y-6">
      <p className="font-serif text-2xl font-semibold">🧬 你的身份</p>
      <p className="text-muted-foreground">个人档案和 Wrapped 身份即将上线...</p>
    </div>
  )
}
```

- [ ] **Step 2: 创建主页面组件**

`frontend/src/pages/AccountCenterPage.tsx`:
```typescript
import { useState } from 'react'
import { cn } from '@/lib/utils'
import { useAccount } from '@/hooks/useAccount'
import { CollectionTab } from '@/pages/account/CollectionTab'
import { HabitsTab } from '@/pages/account/HabitsTab'
import { IdentityTab } from '@/pages/account/IdentityTab'
import { GlassCard } from '@/components/shared/GlassCard'
import type { AccountSummary } from '@/types/account'

type TabKey = 'collection' | 'habits' | 'identity'

const TABS: { key: TabKey; label: string; emoji: string }[] = [
  { key: 'collection', label: '你的收藏', emoji: '📁' },
  { key: 'habits', label: '你的习惯', emoji: '🔍' },
  { key: 'identity', label: '你的身份', emoji: '🧬' },
]

function AccountHero({ data }: { data: AccountSummary }) {
  const profile = data.profile?.profile
  const stats = data.profile?.stats
  const displayName = profile?.identity_displayName || 'Spotify 用户'
  const country = profile?.attr_country || '—'
  const imageUrl = profile?.identity_imageUrl
  const firstName = profile?.identity_firstName
  const lastName = profile?.identity_lastName
  const totalPlays = stats?.total_audio_plays?.toLocaleString() || '—'
  const firstPlayYear = stats?.first_play_date
    ? new Date(stats.first_play_date).getFullYear()
    : null

  // 从 collection insights 获取人格标签
  const personality = data.collection_insights?.available
    ? data.collection_insights.personality.type
    : null

  // 从 habits 推断第二人格
  const searchData = data.search
  const videoData = data.video
  let secondaryPersonality = ''
  if (searchData?.available && videoData?.available) {
    const lateNightRatio = searchData.heatmap?.z
      ? searchData.heatmap.z.reduce((sum, row) => {
          for (let h = 23; h <= 23; h++) sum += row[23] || 0
          for (let h = 0; h < 5; h++) sum += row[h] || 0
          return sum
        }, 0) / Math.max(searchData.total_searches, 1)
      : 0
    if (lateNightRatio > 0.3) secondaryPersonality = '午夜活跃者'
    else if (videoData.total_video_plays > 100) secondaryPersonality = '全媒介达人'
    else secondaryPersonality = '专注聆听者'
  }

  return (
    <GlassCard className="relative mb-8 overflow-hidden border-0 bg-gradient-to-br from-[#1a1a2e] via-[#16213e] to-[#0f3460] px-10 py-10">
      {/* 装饰光晕 */}
      <div className="pointer-events-none absolute -right-10 -top-10 h-[300px] w-[300px] rounded-full bg-[radial-gradient(circle,rgba(180,130,255,0.10)_0%,transparent_70%)]" />
      <div className="pointer-events-none absolute bottom-[-30px] left-[30%] h-[200px] w-[200px] rounded-full bg-[radial-gradient(circle,rgba(100,180,255,0.06)_0%,transparent_70%)]" />

      <div className="relative z-10 flex items-center gap-10">
        {/* 头像 */}
        <div className="h-24 w-24 flex-shrink-0 overflow-hidden rounded-full bg-gradient-to-br from-[#667eea] to-[#764ba2] ring-2 ring-white/20">
          {imageUrl ? (
            <img src={imageUrl} alt={displayName} className="h-full w-full object-cover" />
          ) : (
            <div className="flex h-full w-full items-center justify-center text-4xl">👤</div>
          )}
        </div>

        {/* 信息 */}
        <div className="flex-1">
          <p className="mb-2 font-sans text-[11px] font-bold uppercase tracking-[1.8px] text-white/50">
            你的 Spotify 身份
          </p>
          <h2 className="mb-4 font-serif text-[36px] font-bold leading-[1.06] tracking-[-0.8px] text-white">
            {displayName}
          </h2>
          <div className="flex flex-wrap gap-x-8 gap-y-2">
            {firstName && lastName && (
              <div>
                <span className="text-xs text-white/40">姓名</span>
                <p className="font-medium text-white">{firstName} {lastName}</p>
              </div>
            )}
            <div>
              <span className="text-xs text-white/40">国家</span>
              <p className="font-medium text-white">{country}</p>
            </div>
            {firstPlayYear && (
              <div>
                <span className="text-xs text-white/40">始于</span>
                <p className="font-medium text-white">{firstPlayYear} 年</p>
              </div>
            )}
            <div>
              <span className="text-xs text-white/40">总播放</span>
              <p className="font-medium text-white">{totalPlays}</p>
            </div>
          </div>
        </div>

        {/* 人格标签 */}
        <div className="flex flex-shrink-0 flex-col gap-2">
          {personality && (
            <span className="rounded-full bg-white/12 px-4 py-1.5 text-xs font-semibold text-white">
              {personality}
            </span>
          )}
          {secondaryPersonality && (
            <span className="rounded-full bg-white/12 px-4 py-1.5 text-xs font-semibold text-white">
              {secondaryPersonality}
            </span>
          )}
        </div>
      </div>
    </GlassCard>
  )
}

function LoadingSkeleton() {
  return (
    <div className="mx-auto max-w-[900px] space-y-6 px-6 py-12">
      <div className="h-[180px] animate-pulse rounded-2xl bg-card/50" />
      <div className="flex gap-6 border-b border-border pb-0">
        {[1, 2, 3].map((i) => (
          <div key={i} className="h-8 w-24 animate-pulse rounded bg-card/50" />
        ))}
      </div>
      <div className="space-y-4">
        {[1, 2, 3].map((i) => (
          <div key={i} className="h-48 animate-pulse rounded-xl bg-card/50" />
        ))}
      </div>
    </div>
  )
}

function ErrorState({ error, onRetry }: { error: string; onRetry: () => void }) {
  return (
    <div className="mx-auto flex max-w-[900px] flex-col items-center gap-4 px-6 py-24">
      <p className="text-center text-muted-foreground">加载失败：{error}</p>
      <button
        onClick={onRetry}
        className="rounded-full bg-accent-foreground px-6 py-2 text-[13px] font-semibold text-primary-foreground transition-opacity hover:opacity-90"
      >
        重新加载
      </button>
    </div>
  )
}

function EmptyState() {
  return (
    <div className="mx-auto flex max-w-[900px] flex-col items-center gap-4 px-6 py-24">
      <p className="text-center font-serif text-xl font-semibold">尚未导入账号数据</p>
      <p className="max-w-md text-center text-[15px] leading-relaxed text-muted-foreground">
        前往
        <a href="/settings" className="mx-1 font-medium text-accent-foreground underline underline-offset-2">
          设置页面
        </a>
        导入 Spotify 账号数据（YourLibrary.json、SearchQueries.json 等），即可解锁你的音乐人格档案。
      </p>
    </div>
  )
}

export function AccountCenterPage() {
  const { data, loading, error, refetch } = useAccount()
  const [activeTab, setActiveTab] = useState<TabKey>('collection')

  if (loading) return <LoadingSkeleton />
  if (error) return <ErrorState error={error} onRetry={refetch} />
  if (!data || !data.has_account_data) return <EmptyState />

  return (
    <div className="mx-auto max-w-[900px] space-y-6 px-6 py-12">
      <AccountHero data={data} />

      {/* Tab 导航 */}
      <div className="flex gap-6 border-b border-border">
        {TABS.map((tab) => (
          <button
            key={tab.key}
            onClick={() => setActiveTab(tab.key)}
            className={cn(
              '-mb-px pb-2.5 font-sans text-[13px] font-medium transition-colors border-b-2',
              activeTab === tab.key
                ? 'border-accent-foreground font-semibold text-foreground'
                : 'border-transparent text-muted-foreground hover:text-foreground',
            )}
          >
            {tab.emoji} {tab.label}
          </button>
        ))}
      </div>

      {/* Tab 内容 */}
      <div>
        {activeTab === 'collection' && data.collection_insights?.available && (
          <CollectionTab insights={data.collection_insights} />
        )}
        {activeTab === 'collection' && !data.collection_insights?.available && (
          <div className="py-12 text-center text-muted-foreground">收藏数据不可用</div>
        )}

        {activeTab === 'habits' && data.search?.available && (
          <HabitsTab
            search={data.search}
            tiers={data.insights_tiers}
            marquee={data.insights_marquee}
            podcast={data.podcast}
            video={data.video}
          />
        )}
        {activeTab === 'habits' && !data.search?.available && (
          <div className="py-12 text-center text-muted-foreground">搜索/习惯数据不可用</div>
        )}

        {activeTab === 'identity' && (
          <IdentityTab profile={data.profile} wrappedHub={null} />
        )}
      </div>
    </div>
  )
}
```

- [ ] **Step 2: 验证编译**

```bash
cd frontend && npx tsc --noEmit
```

- [ ] **Step 3: 启动开发服务器验证页面可访问**

```bash
cd frontend && npm run dev &
sleep 3
# 访问 http://localhost:5173/account
```

- [ ] **Step 4: 提交**

```bash
git add frontend/src/pages/AccountCenterPage.tsx \
        frontend/src/pages/account/CollectionTab.tsx \
        frontend/src/pages/account/HabitsTab.tsx \
        frontend/src/pages/account/IdentityTab.tsx
git commit -m "$(cat <<'EOF'
feat: 创建账号中心页面骨架

- AccountCenterPage: Hero + 三 Tab 切换 + 加载/错误/空状态
- AccountHero: 渐变背景 + 头像 + 身份信息 + 人格标签
- CollectionTab / HabitsTab / IdentityTab: 占位组件
EOF
)"
```

---

### Task 7: 前端 — CollectionTab 完整实现

**Files:**
- Modify (rewrite): `frontend/src/pages/account/CollectionTab.tsx`
- 因为单个文件太大（~400 行），所有子区块内联为组件，不需要拆分成单独文件

> **注意**: 以下仅列出关键结构和数据映射，完整 UI 代码按照 `docs/superpowers/specs/2026-05-30-account-center-design.md` Tab 1 的 8 个模块逐一实现。每个模块渲染一个 GlassCard 或 border-card。由于代码量巨大，实现时参考设计文档中的 mockup 线框图，将每一块翻译成 Tailwind + React。

- [ ] **Step 1: 实现收藏人格区块**
- [ ] **Step 2: 实现收藏纵览（KPI + 时间线）**
- [ ] **Step 3: 实现第一首收藏故事 + 收藏夹档案**
- [ ] **Step 4: 实现收藏生命周期四阶段**
- [ ] **Step 5: 实现化学反应六类型网格**
- [ ] **Step 6: 实现另一面 + 品味迁徙 + 双厨时刻**
- [ ] **Step 7: 实现排行榜（Top 艺人 + 错位榜）**
- [ ] **Step 8: 实现浏览器（收藏曲目 + 播放列表）**
- [ ] **Step 9: 验证编译 + 提交**

---

### Task 8: 前端 — HabitsTab 完整实现

**Files:**
- Modify (rewrite): `frontend/src/pages/account/HabitsTab.tsx`

同上，按设计文档 Tab 2 的 6 个模块逐一实现。

---

### Task 9: 前端 — IdentityTab 完整实现

**Files:**
- Modify (rewrite): `frontend/src/pages/account/IdentityTab.tsx`

同上，按设计文档 Tab 3 的 5 个模块逐一实现。

---

## 实现顺序建议

1. Task 1-2 (后端) → 验证 API 返回数据
2. Task 3-4 (类型 + Hook) → 验证数据流
3. Task 5-6 (路由 + 页面骨架) → 验证页面可访问
4. Task 7-9 (三个 Tab) → 逐一填充内容

每个 Task 内部步骤完成后启动一次开发服务器确认不报错。
